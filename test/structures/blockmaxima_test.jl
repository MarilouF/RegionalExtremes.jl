@testset "blockmaximagrid.jl" begin
    @testset "BlockMaximaGrid(m, p, y)" begin
        # TODO : Test prod(m...) != size(y, 1)
        # TODO : Test order 1 (m == gridSize, p == p, y == y)
        # TODO : Test order 2 (m == gridSize, p == p, y == y)
    end
end
